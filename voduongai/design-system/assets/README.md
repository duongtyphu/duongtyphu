# Design Assets

Thư mục chứa asset thiết kế dùng chung (icon set riêng, texture, font đặc biệt...) không thuộc về ảnh Design Reference (xem `/design-system/design-references/`) và không phải asset runtime của app (asset runtime vẫn đặt trong `public/`).
