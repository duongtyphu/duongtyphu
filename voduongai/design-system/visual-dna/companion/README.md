# Companion — Visual DNA

Reference và asset riêng cho **The Sanctuary of Companion** (`/portal/companion`).

- Component đang triển khai: `src/app/portal/companion/page.tsx`, `src/components/portal/sanctuary/` (`SanctuaryBackground.tsx`, `Reveal.tsx`)

## Reference đã duyệt

- **`LIVING-CORE-001.png` / `LIVING-CORE-001.md`** — Companion Living Core™,
  **Design Lock** (đối xử như logo, xem rule trong file `.md`). Component:
  `src/components/LivingCore.tsx`. KHÔNG redesign — chỉ được
  dựng lại bằng SVG + CSS, giữ nguyên tỷ lệ/hình dạng/màu sắc/số lượng quỹ đạo.

Design Spec riêng cho phần còn lại của Companion (`COMPANION_DESIGN_SPEC.md`)
cần được viết khi Founder gửi ảnh kèm "DUYỆT" — các khu vực khác vẫn đang
triển khai theo brief văn bản gốc, chưa có Visual Reference chính thức khóa lại.
