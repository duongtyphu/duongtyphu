# Leaves

Asset lá: leaf chip (UI component), leaf icon, falling leaf shape.

Đặt tên: `leaf-<loại-hành-động>` (`leaf-read`, `leaf-learn`, `leaf-practice`, `leaf-save`, `leaf-ask`, `leaf-share`, `leaf-challenge`, `leaf-explore`).

Lá hành động hiện là UI component thật (không phải ảnh) — xem `src/components/portal/garden/GardenTreeVisual.tsx` (`.garden-leaf-chip`) và quy tắc "Garden Leaf" trong `src/design-system/07-components/README.md`. Hình lá rơi (`FALLING_LEAVES`) dùng SVG path inline, chưa cần asset ảnh riêng.
