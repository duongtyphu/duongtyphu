/**
 * EPIC 02 — Sprint 05: câu nói Companion khi báo Unlock — xem
 * `docs/COMPANION_UNLOCK_LANGUAGE.md` cho quy tắc viết đầy đủ. KHÔNG dùng
 * "Unlocked"/"Reward"/"Claim"/"+XP".
 */

export function readyToReceiveMessage(): string {
  return "Mình nghĩ bây giờ bạn đã sẵn sàng nhận bộ công cụ này.";
}

export function keptForRightMomentMessage(): string {
  return "Mình giữ bộ công cụ này cho đúng thời điểm.";
}

export function afterReflectionMessage(): string {
  return "Sau Reflection này, mình muốn gửi bạn một thứ hữu ích.";
}

export function readyToUseMessage(): string {
  return "Bây giờ bạn có thể dùng bộ này để làm tốt hơn.";
}
